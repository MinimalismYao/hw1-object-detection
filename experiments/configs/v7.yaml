# experiments/configs/v7.yaml
# =========================
# hw1_od · fasterrcnn_v7 (R101 + FPN P2~P6 + Big RoI Head)
# 目標：穩定收斂、避免 OOM、提升小物件 AP
# =========================

seed: 42

project:
  name: hw1_od
  seed: 42
  run_name: fasterrcnn_v7
  output_root: experiments/logs
  output_dir: ${project.output_root}/${project.run_name}

data:
  root: data
  train_img_dir: data/train/img
  train_gt: data/train/gt_train.txt
  val_img_dir: data/val/img
  val_gt: data/val/gt_val.txt
  test_img_dir: data/test/img
  num_workers: 16
  allow_float_bbox: true
  clip_bbox_to_image: true
  drop_invalid_bbox: true
  min_box_wh: [1, 1]

model:
  detector: fasterrcnn_r101_fpn_v7
  num_classes: 2
  image_mean: [0.485, 0.456, 0.406]
  image_std:  [0.229, 0.224, 0.225]

  # 輸入尺度（降到 1024 以省顯存）
  min_size: 1024
  max_size: 1024

  # FPN / Norm
  fpn_out_channels: 256
  use_p6: true
  norm: "groupnorm32"

  # Anchors：小物件友善
  rpn_anchor_sizes:  [8, 12, 16, 24, 32, 48]
  rpn_anchor_ratios: [0.5, 1.0, 2.0, 3.0]

  # RPN：提案數下修，降記憶體負載
  rpn:
    batch_size_per_image: 512
    positive_fraction: 0.5
    fg_iou_thresh: 0.6
    bg_iou_thresh: 0.2
    pre_nms_top_n_train: 3000
    post_nms_top_n_train: 1500
    pre_nms_top_n_test:  2000
    post_nms_top_n_test: 1000
    nms_thresh: 0.7

  # RoI / Head：pool 尺寸與 sample 數下修以防 OOM
  roi:
    roi_pool_size: 11           # 14 -> 11
    head_hidden_dim: 1024
    head_num_fc: 4
    bbox_reg_weights: [10.0, 10.0, 5.0, 5.0]
    nms_thresh: 0.5
    detections_per_img: 150     # 200 -> 150
    score_thresh: 0.0
    box_batch_size_per_image: 512   # 1024 -> 512
    box_positive_fraction: 0.33

augment:
  # 與 1024 對齊；多尺度保守
  resize: [896, 1024]
  max_side: 1024
  flip_p: 0.5
  hsv: [0.015, 0.70, 0.40]
  color_jitter_prob: 0.4
  color_jitter: [0.1, 0.1, 0.1, 0.03]
  mosaic: false

train:
  epochs: 48
  batch_size: 2          # 4 -> 2（單步顯存更低）
  accumulate: 8          # 4 -> 8（維持等效 batch）
  amp: true
  cudnn_benchmark: true
  grad_clip: 1.0

  model_ema:
    enabled: true
    decay: 0.9995

  early_stop:
    enabled: true
    monitor: val_loss
    mode: min
    patience: 8          # 6 -> 8（更穩）
    save_best: true
    best_name: ${project.run_name}_best.pth

optimizer:
  type: sgd
  lr: 0.010              # 等效 batch ≈ 16，0.01 為穩健起點
  momentum: 0.9
  weight_decay: 0.0005

# Cosine + Warmup：loss 更平滑
scheduler:
  name: cosine
  warmup:
    type: linear
    steps: 1000
    start_factor: 0.1

dataloader:
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 2
  shuffle: true

sanity_check:
  enabled: true
  batch_size: 1
  max_side: 640

checkpoint:
  dir: ${project.output_dir}
  name: ${project.run_name}.pth
  save_last_only: false
  save_full_path: ${checkpoint.dir}/${checkpoint.name}

logging:
  csv_name: train_log.csv
  csv_path: ${project.output_dir}/${logging.csv_name}
  print_interval: 10

eval:
  run_after_train: true
  iou_thresholds: [0.50,0.55,0.60,0.65,0.70,0.75,0.80,0.85,0.90,0.95]
  max_det: 300
  save_dir: experiments/eval_results
  result_txt: ${eval.save_dir}/${project.run_name}_results.txt
  result_json: ${eval.save_dir}/${project.run_name}_details.json
  breakdown_by_area: true
  area_ranges:
    small:  [0, 1024]
    medium: [1024, 9216]
    large:  [9216, 1e10]

infer:
  score_thr: 0.25
  nms_iou: 0.5
  submission_csv: submissions/${project.run_name}_submission.csv

device:
  cuda: true
